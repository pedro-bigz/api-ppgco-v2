export enum ROLES {
  Advisor = 'Professor(a)',
  Student = 'Estudante',
  Secretary = 'Secretário(a)',
  Coordinator = 'Coordenador(a)',
}

export enum Permissions {
  List = 'roles.list',
  Read = 'roles.read',
  Create = 'roles.create',
  Update = 'roles.update',
  Delete = 'roles.delete',
}
